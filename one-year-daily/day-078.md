# Day 078 Implementation Runbook

## Outcome Target
- Primary theme: `T3_TERMINAL_AGENTS`
- Intent: Terminal-native autonomous agents and guardrails
- Gate: paper-trading only; no live capital execution
- Required quality bar: all touched tests green plus browser spot-check

## Preconditions
- Backend reachable on `:8000` and frontend on `:3000`
- `M01_PAPER_REQUIRED` remains first execution gate
- All external calls wrapped with retry + bounded timeout
- Any schema changes are backward compatible and migrated safely

## Work Packages
### D078-WP01 — Delivery Unit 1
- Scope statement: integrate `T3_TERMINAL_AGENTS` into concrete backend and UI paths with observability
- Business rationale: reduce operational risk while increasing measurable confidence for day 078
- Source files likely touched: `backend_api.py`, `src/apex/services/*`, `autopilot-local/frontend/app/dashboard/*`
- Data contract impacted: proposal summary, risk metrics snapshot, and stream-safe UI payloads
- Safety check: no bypass of risk gates M01-M09; reject on uncertain settlement evidence
- Performance budget: p95 endpoint latency under 300ms for dashboard-critical requests
- Reliability budget: retries capped and circuit cooldown respected
- Security note: sanitize all untrusted web text before LLM/tool consumption
- Telemetry requirement: increment counters and add structured audit events with correlation id
- Persistence requirement: use `SQLiteStore` methods only; avoid raw sqlite calls in runtime paths
- Integration detail: wire BrightData/market context only as optional soft enhancement
- Agent behavior: autonomous loops must be idempotent and safe to restart
- UI behavior: preserve terminal-shell hydration and avoid blocking render on optional data
- Error policy: degrade gracefully with explicit warning badges and API detail fields
- Test-first step 1: add/adjust unit tests for domain logic before implementation changes
- Test-first step 2: add integration test around endpoint or job orchestration path
- Test-first step 3: add focused Playwright assertion for the operator-visible behavior
- Implementation step 1: update config defaults and feature toggles with safe bounds
- Implementation step 2: implement service-layer method and pure helper functions
- Implementation step 3: propagate response fields through API and frontend model types
- Implementation step 4: include backwards-compatible fallbacks for older payload versions
- Verification command: `pytest -q tests/ -k "risk or arb or brightdata"`
- Verification command: `npx playwright test --grep "Autopilot|Kalshi|Polymarket"`
- Verification command: `curl -sS http://127.0.0.1:8000/health | jq .status`
- Acceptance criterion A: no increase in 5xx rate across dashboard endpoints
- Acceptance criterion B: strategy loop emits expected audit log for completion
- Acceptance criterion C: risk gate failures are explicit and human-readable
- Acceptance criterion D: browser page remains interactive and no fatal console errors
- Rollback plan: isolate changed module behind toggle and revert feature flag default
- Documentation update: append runbook notes to `docs/runtime/` and `docs/scheduler/` when relevant
- Evidence artifact: attach test output snippets and endpoint payload samples
- Metric to watch: `apex_arb_max_edge`, `apex_positions_open`, and request success counters
- Data quality check: remove duplicate opportunities by stable key before rendering
- Settlement quality check: prefer authoritative domains and confidence-based scoring
- Operational drill: stop/start unified stack and verify idempotent recovery
- Review checkpoint: run code review pass and fix high-severity findings
- Sign-off owner: autonomous agent + operator confirmation
- Completion marker: `D078-WP01-DONE` appended to daily changelog entry

### D078-WP02 — Delivery Unit 2
- Scope statement: integrate `T3_TERMINAL_AGENTS` into concrete backend and UI paths with observability
- Business rationale: reduce operational risk while increasing measurable confidence for day 078
- Source files likely touched: `backend_api.py`, `src/apex/services/*`, `autopilot-local/frontend/app/dashboard/*`
- Data contract impacted: proposal summary, risk metrics snapshot, and stream-safe UI payloads
- Safety check: no bypass of risk gates M01-M09; reject on uncertain settlement evidence
- Performance budget: p95 endpoint latency under 300ms for dashboard-critical requests
- Reliability budget: retries capped and circuit cooldown respected
- Security note: sanitize all untrusted web text before LLM/tool consumption
- Telemetry requirement: increment counters and add structured audit events with correlation id
- Persistence requirement: use `SQLiteStore` methods only; avoid raw sqlite calls in runtime paths
- Integration detail: wire BrightData/market context only as optional soft enhancement
- Agent behavior: autonomous loops must be idempotent and safe to restart
- UI behavior: preserve terminal-shell hydration and avoid blocking render on optional data
- Error policy: degrade gracefully with explicit warning badges and API detail fields
- Test-first step 1: add/adjust unit tests for domain logic before implementation changes
- Test-first step 2: add integration test around endpoint or job orchestration path
- Test-first step 3: add focused Playwright assertion for the operator-visible behavior
- Implementation step 1: update config defaults and feature toggles with safe bounds
- Implementation step 2: implement service-layer method and pure helper functions
- Implementation step 3: propagate response fields through API and frontend model types
- Implementation step 4: include backwards-compatible fallbacks for older payload versions
- Verification command: `pytest -q tests/ -k "risk or arb or brightdata"`
- Verification command: `npx playwright test --grep "Autopilot|Kalshi|Polymarket"`
- Verification command: `curl -sS http://127.0.0.1:8000/health | jq .status`
- Acceptance criterion A: no increase in 5xx rate across dashboard endpoints
- Acceptance criterion B: strategy loop emits expected audit log for completion
- Acceptance criterion C: risk gate failures are explicit and human-readable
- Acceptance criterion D: browser page remains interactive and no fatal console errors
- Rollback plan: isolate changed module behind toggle and revert feature flag default
- Documentation update: append runbook notes to `docs/runtime/` and `docs/scheduler/` when relevant
- Evidence artifact: attach test output snippets and endpoint payload samples
- Metric to watch: `apex_arb_max_edge`, `apex_positions_open`, and request success counters
- Data quality check: remove duplicate opportunities by stable key before rendering
- Settlement quality check: prefer authoritative domains and confidence-based scoring
- Operational drill: stop/start unified stack and verify idempotent recovery
- Review checkpoint: run code review pass and fix high-severity findings
- Sign-off owner: autonomous agent + operator confirmation
- Completion marker: `D078-WP02-DONE` appended to daily changelog entry

### D078-WP03 — Delivery Unit 3
- Scope statement: integrate `T3_TERMINAL_AGENTS` into concrete backend and UI paths with observability
- Business rationale: reduce operational risk while increasing measurable confidence for day 078
- Source files likely touched: `backend_api.py`, `src/apex/services/*`, `autopilot-local/frontend/app/dashboard/*`
- Data contract impacted: proposal summary, risk metrics snapshot, and stream-safe UI payloads
- Safety check: no bypass of risk gates M01-M09; reject on uncertain settlement evidence
- Performance budget: p95 endpoint latency under 300ms for dashboard-critical requests
- Reliability budget: retries capped and circuit cooldown respected
- Security note: sanitize all untrusted web text before LLM/tool consumption
- Telemetry requirement: increment counters and add structured audit events with correlation id
- Persistence requirement: use `SQLiteStore` methods only; avoid raw sqlite calls in runtime paths
- Integration detail: wire BrightData/market context only as optional soft enhancement
- Agent behavior: autonomous loops must be idempotent and safe to restart
- UI behavior: preserve terminal-shell hydration and avoid blocking render on optional data
- Error policy: degrade gracefully with explicit warning badges and API detail fields
- Test-first step 1: add/adjust unit tests for domain logic before implementation changes
- Test-first step 2: add integration test around endpoint or job orchestration path
- Test-first step 3: add focused Playwright assertion for the operator-visible behavior
- Implementation step 1: update config defaults and feature toggles with safe bounds
- Implementation step 2: implement service-layer method and pure helper functions
- Implementation step 3: propagate response fields through API and frontend model types
- Implementation step 4: include backwards-compatible fallbacks for older payload versions
- Verification command: `pytest -q tests/ -k "risk or arb or brightdata"`
- Verification command: `npx playwright test --grep "Autopilot|Kalshi|Polymarket"`
- Verification command: `curl -sS http://127.0.0.1:8000/health | jq .status`
- Acceptance criterion A: no increase in 5xx rate across dashboard endpoints
- Acceptance criterion B: strategy loop emits expected audit log for completion
- Acceptance criterion C: risk gate failures are explicit and human-readable
- Acceptance criterion D: browser page remains interactive and no fatal console errors
- Rollback plan: isolate changed module behind toggle and revert feature flag default
- Documentation update: append runbook notes to `docs/runtime/` and `docs/scheduler/` when relevant
- Evidence artifact: attach test output snippets and endpoint payload samples
- Metric to watch: `apex_arb_max_edge`, `apex_positions_open`, and request success counters
- Data quality check: remove duplicate opportunities by stable key before rendering
- Settlement quality check: prefer authoritative domains and confidence-based scoring
- Operational drill: stop/start unified stack and verify idempotent recovery
- Review checkpoint: run code review pass and fix high-severity findings
- Sign-off owner: autonomous agent + operator confirmation
- Completion marker: `D078-WP03-DONE` appended to daily changelog entry

### D078-WP04 — Delivery Unit 4
- Scope statement: integrate `T3_TERMINAL_AGENTS` into concrete backend and UI paths with observability
- Business rationale: reduce operational risk while increasing measurable confidence for day 078
- Source files likely touched: `backend_api.py`, `src/apex/services/*`, `autopilot-local/frontend/app/dashboard/*`
- Data contract impacted: proposal summary, risk metrics snapshot, and stream-safe UI payloads
- Safety check: no bypass of risk gates M01-M09; reject on uncertain settlement evidence
- Performance budget: p95 endpoint latency under 300ms for dashboard-critical requests
- Reliability budget: retries capped and circuit cooldown respected
- Security note: sanitize all untrusted web text before LLM/tool consumption
- Telemetry requirement: increment counters and add structured audit events with correlation id
- Persistence requirement: use `SQLiteStore` methods only; avoid raw sqlite calls in runtime paths
- Integration detail: wire BrightData/market context only as optional soft enhancement
- Agent behavior: autonomous loops must be idempotent and safe to restart
- UI behavior: preserve terminal-shell hydration and avoid blocking render on optional data
- Error policy: degrade gracefully with explicit warning badges and API detail fields
- Test-first step 1: add/adjust unit tests for domain logic before implementation changes
- Test-first step 2: add integration test around endpoint or job orchestration path
- Test-first step 3: add focused Playwright assertion for the operator-visible behavior
- Implementation step 1: update config defaults and feature toggles with safe bounds
- Implementation step 2: implement service-layer method and pure helper functions
- Implementation step 3: propagate response fields through API and frontend model types
- Implementation step 4: include backwards-compatible fallbacks for older payload versions
- Verification command: `pytest -q tests/ -k "risk or arb or brightdata"`
- Verification command: `npx playwright test --grep "Autopilot|Kalshi|Polymarket"`
- Verification command: `curl -sS http://127.0.0.1:8000/health | jq .status`
- Acceptance criterion A: no increase in 5xx rate across dashboard endpoints
- Acceptance criterion B: strategy loop emits expected audit log for completion
- Acceptance criterion C: risk gate failures are explicit and human-readable
- Acceptance criterion D: browser page remains interactive and no fatal console errors
- Rollback plan: isolate changed module behind toggle and revert feature flag default
- Documentation update: append runbook notes to `docs/runtime/` and `docs/scheduler/` when relevant
- Evidence artifact: attach test output snippets and endpoint payload samples
- Metric to watch: `apex_arb_max_edge`, `apex_positions_open`, and request success counters
- Data quality check: remove duplicate opportunities by stable key before rendering
- Settlement quality check: prefer authoritative domains and confidence-based scoring
- Operational drill: stop/start unified stack and verify idempotent recovery
- Review checkpoint: run code review pass and fix high-severity findings
- Sign-off owner: autonomous agent + operator confirmation
- Completion marker: `D078-WP04-DONE` appended to daily changelog entry

### D078-WP05 — Delivery Unit 5
- Scope statement: integrate `T3_TERMINAL_AGENTS` into concrete backend and UI paths with observability
- Business rationale: reduce operational risk while increasing measurable confidence for day 078
- Source files likely touched: `backend_api.py`, `src/apex/services/*`, `autopilot-local/frontend/app/dashboard/*`
- Data contract impacted: proposal summary, risk metrics snapshot, and stream-safe UI payloads
- Safety check: no bypass of risk gates M01-M09; reject on uncertain settlement evidence
- Performance budget: p95 endpoint latency under 300ms for dashboard-critical requests
- Reliability budget: retries capped and circuit cooldown respected
- Security note: sanitize all untrusted web text before LLM/tool consumption
- Telemetry requirement: increment counters and add structured audit events with correlation id
- Persistence requirement: use `SQLiteStore` methods only; avoid raw sqlite calls in runtime paths
- Integration detail: wire BrightData/market context only as optional soft enhancement
- Agent behavior: autonomous loops must be idempotent and safe to restart
- UI behavior: preserve terminal-shell hydration and avoid blocking render on optional data
- Error policy: degrade gracefully with explicit warning badges and API detail fields
- Test-first step 1: add/adjust unit tests for domain logic before implementation changes
- Test-first step 2: add integration test around endpoint or job orchestration path
- Test-first step 3: add focused Playwright assertion for the operator-visible behavior
- Implementation step 1: update config defaults and feature toggles with safe bounds
- Implementation step 2: implement service-layer method and pure helper functions
- Implementation step 3: propagate response fields through API and frontend model types
- Implementation step 4: include backwards-compatible fallbacks for older payload versions
- Verification command: `pytest -q tests/ -k "risk or arb or brightdata"`
- Verification command: `npx playwright test --grep "Autopilot|Kalshi|Polymarket"`
- Verification command: `curl -sS http://127.0.0.1:8000/health | jq .status`
- Acceptance criterion A: no increase in 5xx rate across dashboard endpoints
- Acceptance criterion B: strategy loop emits expected audit log for completion
- Acceptance criterion C: risk gate failures are explicit and human-readable
- Acceptance criterion D: browser page remains interactive and no fatal console errors
- Rollback plan: isolate changed module behind toggle and revert feature flag default
- Documentation update: append runbook notes to `docs/runtime/` and `docs/scheduler/` when relevant
- Evidence artifact: attach test output snippets and endpoint payload samples
- Metric to watch: `apex_arb_max_edge`, `apex_positions_open`, and request success counters
- Data quality check: remove duplicate opportunities by stable key before rendering
- Settlement quality check: prefer authoritative domains and confidence-based scoring
- Operational drill: stop/start unified stack and verify idempotent recovery
- Review checkpoint: run code review pass and fix high-severity findings
- Sign-off owner: autonomous agent + operator confirmation
- Completion marker: `D078-WP05-DONE` appended to daily changelog entry

### D078-WP06 — Delivery Unit 6
- Scope statement: integrate `T3_TERMINAL_AGENTS` into concrete backend and UI paths with observability
- Business rationale: reduce operational risk while increasing measurable confidence for day 078
- Source files likely touched: `backend_api.py`, `src/apex/services/*`, `autopilot-local/frontend/app/dashboard/*`
- Data contract impacted: proposal summary, risk metrics snapshot, and stream-safe UI payloads
- Safety check: no bypass of risk gates M01-M09; reject on uncertain settlement evidence
- Performance budget: p95 endpoint latency under 300ms for dashboard-critical requests
- Reliability budget: retries capped and circuit cooldown respected
- Security note: sanitize all untrusted web text before LLM/tool consumption
- Telemetry requirement: increment counters and add structured audit events with correlation id
- Persistence requirement: use `SQLiteStore` methods only; avoid raw sqlite calls in runtime paths
- Integration detail: wire BrightData/market context only as optional soft enhancement
- Agent behavior: autonomous loops must be idempotent and safe to restart
- UI behavior: preserve terminal-shell hydration and avoid blocking render on optional data
- Error policy: degrade gracefully with explicit warning badges and API detail fields
- Test-first step 1: add/adjust unit tests for domain logic before implementation changes
- Test-first step 2: add integration test around endpoint or job orchestration path
- Test-first step 3: add focused Playwright assertion for the operator-visible behavior
- Implementation step 1: update config defaults and feature toggles with safe bounds
- Implementation step 2: implement service-layer method and pure helper functions
- Implementation step 3: propagate response fields through API and frontend model types
- Implementation step 4: include backwards-compatible fallbacks for older payload versions
- Verification command: `pytest -q tests/ -k "risk or arb or brightdata"`
- Verification command: `npx playwright test --grep "Autopilot|Kalshi|Polymarket"`
- Verification command: `curl -sS http://127.0.0.1:8000/health | jq .status`
- Acceptance criterion A: no increase in 5xx rate across dashboard endpoints
- Acceptance criterion B: strategy loop emits expected audit log for completion
- Acceptance criterion C: risk gate failures are explicit and human-readable
- Acceptance criterion D: browser page remains interactive and no fatal console errors
- Rollback plan: isolate changed module behind toggle and revert feature flag default
- Documentation update: append runbook notes to `docs/runtime/` and `docs/scheduler/` when relevant
- Evidence artifact: attach test output snippets and endpoint payload samples
- Metric to watch: `apex_arb_max_edge`, `apex_positions_open`, and request success counters
- Data quality check: remove duplicate opportunities by stable key before rendering
- Settlement quality check: prefer authoritative domains and confidence-based scoring
- Operational drill: stop/start unified stack and verify idempotent recovery
- Review checkpoint: run code review pass and fix high-severity findings
- Sign-off owner: autonomous agent + operator confirmation
- Completion marker: `D078-WP06-DONE` appended to daily changelog entry

### D078-WP07 — Delivery Unit 7
- Scope statement: integrate `T3_TERMINAL_AGENTS` into concrete backend and UI paths with observability
- Business rationale: reduce operational risk while increasing measurable confidence for day 078
- Source files likely touched: `backend_api.py`, `src/apex/services/*`, `autopilot-local/frontend/app/dashboard/*`
- Data contract impacted: proposal summary, risk metrics snapshot, and stream-safe UI payloads
- Safety check: no bypass of risk gates M01-M09; reject on uncertain settlement evidence
- Performance budget: p95 endpoint latency under 300ms for dashboard-critical requests
- Reliability budget: retries capped and circuit cooldown respected
- Security note: sanitize all untrusted web text before LLM/tool consumption
- Telemetry requirement: increment counters and add structured audit events with correlation id
- Persistence requirement: use `SQLiteStore` methods only; avoid raw sqlite calls in runtime paths
- Integration detail: wire BrightData/market context only as optional soft enhancement
- Agent behavior: autonomous loops must be idempotent and safe to restart
- UI behavior: preserve terminal-shell hydration and avoid blocking render on optional data
- Error policy: degrade gracefully with explicit warning badges and API detail fields
- Test-first step 1: add/adjust unit tests for domain logic before implementation changes
- Test-first step 2: add integration test around endpoint or job orchestration path
- Test-first step 3: add focused Playwright assertion for the operator-visible behavior
- Implementation step 1: update config defaults and feature toggles with safe bounds
- Implementation step 2: implement service-layer method and pure helper functions
- Implementation step 3: propagate response fields through API and frontend model types
- Implementation step 4: include backwards-compatible fallbacks for older payload versions
- Verification command: `pytest -q tests/ -k "risk or arb or brightdata"`
- Verification command: `npx playwright test --grep "Autopilot|Kalshi|Polymarket"`
- Verification command: `curl -sS http://127.0.0.1:8000/health | jq .status`
- Acceptance criterion A: no increase in 5xx rate across dashboard endpoints
- Acceptance criterion B: strategy loop emits expected audit log for completion
- Acceptance criterion C: risk gate failures are explicit and human-readable
- Acceptance criterion D: browser page remains interactive and no fatal console errors
- Rollback plan: isolate changed module behind toggle and revert feature flag default
- Documentation update: append runbook notes to `docs/runtime/` and `docs/scheduler/` when relevant
- Evidence artifact: attach test output snippets and endpoint payload samples
- Metric to watch: `apex_arb_max_edge`, `apex_positions_open`, and request success counters
- Data quality check: remove duplicate opportunities by stable key before rendering
- Settlement quality check: prefer authoritative domains and confidence-based scoring
- Operational drill: stop/start unified stack and verify idempotent recovery
- Review checkpoint: run code review pass and fix high-severity findings
- Sign-off owner: autonomous agent + operator confirmation
- Completion marker: `D078-WP07-DONE` appended to daily changelog entry

### D078-WP08 — Delivery Unit 8
- Scope statement: integrate `T3_TERMINAL_AGENTS` into concrete backend and UI paths with observability
- Business rationale: reduce operational risk while increasing measurable confidence for day 078
- Source files likely touched: `backend_api.py`, `src/apex/services/*`, `autopilot-local/frontend/app/dashboard/*`
- Data contract impacted: proposal summary, risk metrics snapshot, and stream-safe UI payloads
- Safety check: no bypass of risk gates M01-M09; reject on uncertain settlement evidence
- Performance budget: p95 endpoint latency under 300ms for dashboard-critical requests
- Reliability budget: retries capped and circuit cooldown respected
- Security note: sanitize all untrusted web text before LLM/tool consumption
- Telemetry requirement: increment counters and add structured audit events with correlation id
- Persistence requirement: use `SQLiteStore` methods only; avoid raw sqlite calls in runtime paths
- Integration detail: wire BrightData/market context only as optional soft enhancement
- Agent behavior: autonomous loops must be idempotent and safe to restart
- UI behavior: preserve terminal-shell hydration and avoid blocking render on optional data
- Error policy: degrade gracefully with explicit warning badges and API detail fields
- Test-first step 1: add/adjust unit tests for domain logic before implementation changes
- Test-first step 2: add integration test around endpoint or job orchestration path
- Test-first step 3: add focused Playwright assertion for the operator-visible behavior
- Implementation step 1: update config defaults and feature toggles with safe bounds
- Implementation step 2: implement service-layer method and pure helper functions
- Implementation step 3: propagate response fields through API and frontend model types
- Implementation step 4: include backwards-compatible fallbacks for older payload versions
- Verification command: `pytest -q tests/ -k "risk or arb or brightdata"`
- Verification command: `npx playwright test --grep "Autopilot|Kalshi|Polymarket"`
- Verification command: `curl -sS http://127.0.0.1:8000/health | jq .status`
- Acceptance criterion A: no increase in 5xx rate across dashboard endpoints
- Acceptance criterion B: strategy loop emits expected audit log for completion
- Acceptance criterion C: risk gate failures are explicit and human-readable
- Acceptance criterion D: browser page remains interactive and no fatal console errors
- Rollback plan: isolate changed module behind toggle and revert feature flag default
- Documentation update: append runbook notes to `docs/runtime/` and `docs/scheduler/` when relevant
- Evidence artifact: attach test output snippets and endpoint payload samples
- Metric to watch: `apex_arb_max_edge`, `apex_positions_open`, and request success counters
- Data quality check: remove duplicate opportunities by stable key before rendering
- Settlement quality check: prefer authoritative domains and confidence-based scoring
- Operational drill: stop/start unified stack and verify idempotent recovery
- Review checkpoint: run code review pass and fix high-severity findings
- Sign-off owner: autonomous agent + operator confirmation
- Completion marker: `D078-WP08-DONE` appended to daily changelog entry

## End-of-Day Verification Matrix
- Check 01: objective evidence recorded for daily requirement 01 with command output and notes
- Check 02: objective evidence recorded for daily requirement 02 with command output and notes
- Check 03: objective evidence recorded for daily requirement 03 with command output and notes
- Check 04: objective evidence recorded for daily requirement 04 with command output and notes
- Check 05: objective evidence recorded for daily requirement 05 with command output and notes
- Check 06: objective evidence recorded for daily requirement 06 with command output and notes
- Check 07: objective evidence recorded for daily requirement 07 with command output and notes
- Check 08: objective evidence recorded for daily requirement 08 with command output and notes
- Check 09: objective evidence recorded for daily requirement 09 with command output and notes
- Check 10: objective evidence recorded for daily requirement 10 with command output and notes
- Check 11: objective evidence recorded for daily requirement 11 with command output and notes
- Check 12: objective evidence recorded for daily requirement 12 with command output and notes
- Check 13: objective evidence recorded for daily requirement 13 with command output and notes
- Check 14: objective evidence recorded for daily requirement 14 with command output and notes
- Check 15: objective evidence recorded for daily requirement 15 with command output and notes

## Daily Changelog Template
- What changed:
- Why it changed:
- Tests executed:
- Browser checks executed:
- Risks introduced:
- Rollback readiness:
