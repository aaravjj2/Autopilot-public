# Cycle Report — 2026-06-08 14:10 UTC

## Tag
cycle-20260608-1410

## Changes Committed
3 test fixes (commit `0971a08`):

### 1. test_001_regression.py — `_rg_python_fallback`
Added a pure-Python fallback for `rg` (ripgrep) using `Path.rglob` so the regression
harness works on GitHub Actions and systems without ripgrep installed.

### 2. test_dashboard_health.py — FinanceBrain mock
Fixed `test_llm_route_ok_when_groq_key_present` to use a `SimpleNamespace` mock
for `FinanceBrain` via `monkeypatch.setattr`, avoiding real LLM client init during
the test.

### 3. test_pm_trading.py — settings cache clear
Added `get_settings.cache_clear()` in `test_build_engine_has_pm_brokers` to prevent
cross-test cache poisoning of the settings singleton.

## Test Results
- **385 passed** (all non-slow tests)
- **20 skipped** (Playwright E2E — no dashboard server running)
- **9 deselected** (slow/integration tests excluded per `-m "not slow"`)
- **0 failed**
- Duration: 2m 52s

## Next Steps
- Run the 9 slow tests in a separate pass
- Verify Playwright E2E tests with a running dashboard server
